name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node: [20]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - run: corepack enable  # Enable pnpm

      - run: pnpm --version  # Verify installation

      - name: 📦 Install dependencies
        run: pnpm install

      - name: ✅ Lint
        run: pnpm lint

      # - name: 🧪 Test project
      #   run: pnpm test
